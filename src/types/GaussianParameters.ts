export interface GaussianParameters {
  amplitude: number;  // Height of the Gaussian peak
  centerX: number;    // X coordinate of the center
  centerY: number;    // Y coordinate of the center
  sigma: number;      // Standard deviation (spread) of the Gaussian
}
