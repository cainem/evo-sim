export interface OrganismParameters {
  x: number;
  y: number;
  roundsLived?: number;  // Optional, for initial organisms
  deliberateMutationX: number;
  deliberateMutationY: number;
  offspringsXDistance: number;
  offspringsYDistance: number;
}
